# Webpack Demo

To start a webpack-dev-server you need following steps:

1) Install [npm](https://www.npmjs.com/get-npm)
2) Install packages with `npm ci`
3) Run webpack dev server with `npm start`, which will open your default browser with the page `localhost:3505`
